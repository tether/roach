#!/usr/bin/env node

/**
 * Module dependencies.
 */

var roach = require('..');
var path = require('path');

var options = process.argv.slice(2)[0];
if (options) options = JSON.parse(options);

roach(options)
  .scan(path.resolve('./example'));