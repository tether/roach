#!/usr/bin/env node

/**
 * Module dependencies.
 */

var roach = require('..');
var path = require('path');

var conf = process.argv.slice(2)[0];
if(conf) conf = JSON.parse(conf);

roach(conf)
  .scan(path.resolve('./example'));